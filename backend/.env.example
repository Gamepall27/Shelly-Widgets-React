# HTTP server
PORT=3000
WS_PATH=/ws

# MQTT broker (Mosquitto default on Windows/Pi)
# Use 127.0.0.1 to avoid IPv6 issues with localhost.
MQTT_URL=mqtt://127.0.0.1:1883

# Shelly MQTT Prefix / Device name.
# For testing without Shelly: shellyTEST
MQTT_PREFIX=shellyTEST

# HTTP-RPC base URL (Gen3). Example: http://192.168.1.10
# Optional if SHELLY_DEVICES is set.
SHELLY_BASE_URL=

# Optional: list of Shelly devices (id|name|baseUrl, comma-separated)
# Example: 1|Wohnzimmer|http://192.168.1.10,2|Garage|http://192.168.1.11
SHELLY_DEVICES=

# Client name used as "src" in Shelly RPC messages.
RPC_SRC=pi-dashboard

# Polling via RPC (seconds). 0 = off
STATUS_POLL_SEC=0

# Keep only newest N topics
MAX_TOPICS=250

# Optional: serve built React frontend from backend/public
SERVE_FRONTEND=0
FRONTEND_DIR=public
